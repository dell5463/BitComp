{
  "name": "lr_schedule",
  "output": "results/lr_schedule",
  "description": "Stage 8a: learning-rate schedule only (constant vs cosine vs ReduceLROnPlateau); TBPTT chunk fixed at 256. Base = the milestone winner.",
  "data": {"train_size": 45000, "val_size": 256, "validation_size": 5000},
  "model": {"embedding_dim": 32, "hidden_size": 128, "num_layers": 2, "plane_dim": 8, "row_dim": 8, "col_dim": 8},
  "training": {"epochs": 100000, "max_steps": 6400, "val_every": 800, "batch_size": 32, "chunk_length": 256,
               "learning_rate": 0.001, "mode": "teacher_forcing", "objective": "bce", "lr_schedule": "constant",
               "device": "auto", "seed": 42},
  "sweep": {"split": "val", "size": 200, "engine": "qgru-v2"},
  "variants": [
    {"name": "constant"},
    {"name": "cosine", "training": {"lr_schedule": "cosine"}},
    {"name": "plateau", "training": {"lr_schedule": "plateau"}}
  ]
}

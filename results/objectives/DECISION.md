# Objectives (GPU PC, teacher forcing, bit_plane_rowcol, train_size 1,024, 64,000 steps) — decision

Tables: `SUMMARY.md` (generated by `scripts/summarize.py compare|rate|paired|matched`).
Sample: 200 validation images (development scale), one training seed per variant.

**Winner: bce (uniform BCE).** This is the Milestone 1 final model:
`results/objectives/bce/train/best.pt` (fingerprint `19cbf2c5f8df85f9...`, step 40,000; bit-identical
to `training_modes/teacher_forcing` and `position_ablation/bit_plane_rowcol`).

Evidence (rate at quality, paired image bootstrap vs bce; positive = more bits): weighted_sqrt needs
more bits at every target and both payload codings; all 95% CIs exclude 0:

- raw: +0.08 / +0.05 / +0.04 / +0.22 bpp at 20 / 25 / 30 / 35 dB;
- range-coded: +0.02 / +0.04 / +0.03 / +0.03 bpp.

The envelope-mean metric favours weighted_sqrt (+0.39 dB raw, +1.23 dB range-coded), driven by
the sub-15 dB region; not used for decisions (see `results/position_ablation/DECISION.md`).
Diagnostics only: val BCE 0.4654 vs 0.4681. Checkpoint selection for weighted_sqrt uses its own
(weighted) validation objective.
